#include <stdio.h>

void calculate(int a, int b, int *sum, float *avg)
{
	*sum= a+b;
	*avg= (float)*sum/2;
	
}

int main()
{
	int a=10, b=5;
	int sum=0;
	float avg=0;
	
	calculate(a,b,&sum,&avg);
	printf("The sum is: %d\n",sum);
	printf("The average is: %.2f\n",avg);
	
return 0;
}
